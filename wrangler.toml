name = "saju"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

main = ".open-next/worker.js"
assets = { directory = ".open-next/assets" }

[vars]
NEXT_PUBLIC_APP_URL = "https://saju.pages.dev"

# 로컬 개발 시 환경 변수는 .dev.vars 파일에 정의
# (절대 git에 커밋하지 말 것)
